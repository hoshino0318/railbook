<%= stylesheet_link_tag '/stylesheets/search' %>
<h1> 検索結果 </h1>

<% if @books.size != 0 %>
<table border="1">
  <% @books.each do |book| %>
  <tr>
    <td> <%= book.isbn %></td>
    <td> <%= book.title %></td>
    <td> <%= book.price %></td>
    <td> <%= book.publish %></td>
    <td> <%= book.published %></td>
    <td> <%= book.cd %></td>
  </tr>
  <% end %>
</table>
<% else %>
検索範囲に該当する項目がありません <br/>
<% end %>

<%= link_to '戻る', books_path %>
